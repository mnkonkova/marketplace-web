export const environment = {
  production: false,
  apiBaseUrl: '/api/v1',
  /**
   * База для ссылки-визитки специалиста. Пусто = текущий origin, то есть
   * обычная ссылка вида https://wayprmarket.ru/specialist/<handle>.
   *
   * Зачем вообще отдельная база: краулер превью у Telegram не дотягивается
   * ни до нашего VDS, ни до CDN Яндекса — маршрута до этих сетей у него нет
   * (ВК и WhatsApp ходят свободно, проверено по access-логу Caddy). Ссылка
   * разворачивалась без карточки. Обход — воркер Cloudflare: боту он отдаёт
   * мету и проксирует картинку, человека редиректит на основной сайт.
   * Пользователи при этом ходят на сервер напрямую, без прокси в середине.
   *
   * Формат значения: 'https://<воркер>' без слэша на конце. Путь /s/<handle>
   * добавляется в specialist-profile.page.ts.
   */
  shareBaseUrl: '',
  /**
   * Вход через Яндекс. Пусто = кнопка не показывается: локальная разработка
   * без OAuth это нормальный режим. redirect_uri обязан совпадать с
   * зарегистрированным в кабинете Яндекса посимвольно.
   */
  yandexClientId: '',
  yandexRedirectUri: 'http://192.168.64.2:4200',
};
