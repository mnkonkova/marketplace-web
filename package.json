{
  "name": "marketpclce-web",
  "private": true,
  "version": "0.0.0",
  "description": "Tailwind CSS build for the marketpclce frontend.",
  "scripts": {
    "build:css": "tailwindcss -i ./web/src/tailwind.css -o ./web/styles.css --minify",
    "watch:css": "tailwindcss -i ./web/src/tailwind.css -o ./web/styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
